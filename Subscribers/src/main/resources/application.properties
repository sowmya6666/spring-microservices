spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:mysql://localhost:3306/codeigniter
spring.datasource.username=root
spring.datasource.password=sowmya
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
debug=true
### Spring properties
# Service registers under this name
#spring:
 # application:
  #  name: Subscribers-microservice
spring.application.name=SubscriberService
# Discovery Server Access
#eureka:
   #client:
     # serviceUrl:
     #  defaultZone: http://localhost:1111/eureka/
    eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

 #HTTP Server (Tomcat) Port
#server: 
 # port: 2222
 server.port=8086
 
 #spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.web.servlet.WebMvcAutoConfiguration,org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration

# Disable Spring Boot's "Whitelabel" default error page, so we can use our own
#error:
 # whitelabel:
   # enabled: false
